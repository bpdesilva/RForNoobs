#Parse in arguments to the function
sum_a_b <-function(a,b) {
  result <- a + b #calculation
  return (result) #return the result
}
sum_a_b(10,5)#Arguments for the function

#Parse in arguments to the function
pow <- function(a,b){
  result <- a^b#calculation
  return(result)#return the result
}
pow(10,3)#Arguments for the function
